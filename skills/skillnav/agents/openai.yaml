interface:
  display_name: "SkillNav"
  short_description: "One entry to find, review, use and create useful skills"
  default_prompt: "Use $skillnav to complete my task, finding and reviewing useful skills as needed; create a tested reusable skill when I request one."
policy:
  allow_implicit_invocation: false
