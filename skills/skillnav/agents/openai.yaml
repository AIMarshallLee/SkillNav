interface:
  display_name: "SkillNav"
  short_description: "Decide when skills help, find them, show checked results"
  default_prompt: "Use $skillnav to decide whether a skill helps this task, find and use an eligible skill when needed, and show the checked result."
policy:
  allow_implicit_invocation: false
