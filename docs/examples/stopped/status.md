# Normalization status

Normalization is incomplete.

The approved parser was run once against `input.raw` and failed with exit code 2:
`Unsupported format: this parser has no decoder for this input`.

Per the task boundary, no alternative parser, input invention, source change, or retry was used. The source remains preserved and no normalized output was produced.
